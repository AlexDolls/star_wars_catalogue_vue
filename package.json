{
  "name": "base_template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp 404.html dist/ 2>/dev/null || copy 404.html dist\\ 2>nul || echo 'Note: Copy 404.html to dist folder manually for GitHub Pages'",
    "preview": "vite preview",
    "deploy": "npm run build && echo 'Build complete! Deploy the dist folder to GitHub Pages.'"
  },
  "dependencies": {
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.11",
    "vite-plugin-vue-devtools": "^8.0.3"
  }
}
